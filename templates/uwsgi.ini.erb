[uwsgi]
strict = true
uid = uwsgi
gid = uwsgi
pidfile = /run/uwsgi/uwsgi.pid
emperor = <%= @emperor_dir %>
stats = /run/uwsgi/stats.sock
emperor-tyrant = true
emperor-tyrant-initgroups = true
cap = setgid,setuid